# Environment Variables for Pluto Slider Server
# 
# This file serves as a template for local secrets.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT: Never commit the .env file to version control!

# GitHub Personal Access Token
# Required permissions: repo (for private repositories) or public_repo (for public only)
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_personal_access_token_here

# Optional: Override default configuration paths
# (Leave these commented unless you need custom paths)

# REPOS_CONFIG_PATH=/app/config/repos.yaml
# DATA_DIR=/app/data
# REPOS_DIR=/app/data/repos
# INDEX_DIR=/app/data/index
# SERVER_HOST=0.0.0.0
# SERVER_PORT=2345